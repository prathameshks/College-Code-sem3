#include <iostream>
#include <graphics.h>
#include<math.h>
using namespace std;

enum side{UP,DOWN,RIGHT,LEFT};

void hilbert_curve(int x1,int y1,int length,side s,int iter){
    if(iter == 0){
    
    }

    if (side == UP){
    
    }else if(side == DOWN){
    
    }else if(side == RIGHT){
    
    }else if(side == LEFT){
    
    }
}

int main()
{
    int gd = DETECT,gm;
    initgraph(&gd,&gm,NULL);
    int iterations=5;
    //cin>>iterations;
    
    getch();
    closegraph();
    return 0;
}
